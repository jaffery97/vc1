{
  "name": "livestream-server",
  "homepage": "https://jaffery97.github.io/vc/src/",
  "version": "0.0.1",
  "description": "A conference call implementation using WebRTC, Socket.io and Node.js.",
  "main": "src/app.js",
  "dependencies": {
    "add": "^2.0.6",
    "express": "^4.17.1",
    "gh-pages": "^3.1.0",
    "socket.io": "^2.3.0",
    "yarn": "^1.22.4"
  },
  "devDependencies": {
    "nodemon": "^2.0.2"
  },
  "scripts": {
    "start": "nodemon start",
    "build": "nodemon build",
    "predeploy": "yarn run build",
   "deploy": "gh-pages -d build",
    "eject": "nodemon eject",
    "watch": "nodemon --watch \"src/\" --exec \"node src/app.js\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "LiveStream",
    "Server"
  ],

}
